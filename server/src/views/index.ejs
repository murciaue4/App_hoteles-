<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>imgLoader</title>
    <style>
        body {
            text-align: center;
            background-color: #f2f2f2;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        h1 {
            font-size: 24px;
            color: #333;
            margin-top: 20px;
        }

        form {
            margin-top: 20px;
        }

        input[type="file"] {
            display: block;
            margin: 10px auto;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h1>img shares</h1>
    <form action="/user/upload" method="POST" enctype="multipart/form-data" >
        <input type="file" name="image">
        <button type="submit">Submit</button>
    </form>


    <form action="/user/images" method="GET" enctype="multipart/form-data">
        <button type="submit">getImages</button>
    </form>


</body>

</html>
<!-- hazme un componente de funcion de react js que sea un formulario para agregar un nuevo hotel, debe recolectar un objeto para enviarle a una base de datos,  los datos que espera la peticion son ({
    name: varchar,
    location: int,
    capacity: int,
    type: varchar,
    camas:int,
    baño_privado: 1 ó 0,
    wifi: 1 ó 0,
    restaurant: 1 ó 0,
    lavanderia: 1 ó 0,
    parqueadero: 1 ó 0,
    precio_por_habitacion: int,


});

los campos que esperan un uno o un 0 es decir true o false quiero que los obtengas con inputs tipo checkbox, 
me dejas la funcion onsubmit iniciada, solo escribes el preventDefault() y yo hago el resto
-->